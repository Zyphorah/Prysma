
Ok, lexer avec des optimisations vraiment agressives, tous les tests d'intégration passent au vert, ce qui est bon.

Points négatifs
Par contre, quelques problèmes :
Dans le langage Prysma, lorsque j'ai un int32 variable, le système DFA actuel peut prédire ce token même s'il contient des erreurs.
Exemple : in32 sera considéré comme int32, ce qui est un grave problème.
J'ai vu que le DFA casse les appels de print dans le langage Prysma, je ne sais pas exactement pour quelle raison, à investiguer.
Bon, autre problème, peut-être un oubli, ou simplement du fait que le système ligne/colonne ne fonctionnait pas de base.
Je ne sais pas. Car actuellement, line et column sont initialisés à 0, ce qui cause problème.
Bon, l'utilisation de std::free(tokensData); malloc me semble risquée, mais si c'est pour gagner en performance, pourquoi pas.
Comparer des adresses mémoire, c'est vraiment illisible, bon j'imagine qu'il a fait ça pour la performance brute, il y a sans doute une solution pour générer ces
adresses de façon dynamique sans calculer manuellement if (v == 0x63696C627570)
Bon, il y a les macros, il y a sans doute mieux que ça, je ne sais pas, peut-être une raison de performance encore.
Le fichier src/prysma_code_tests/test_pointer/main.p ne compile même pas
Erreur :
terminate called after throwing an instance of 'std::invalid_argument'
what():  Unknown element in registry: int32
Cause la plus probable : ?

Bon, le goto à la ligne 371 ?? goto end_lexing; c'est bizarre, sans doute pour les performances


Points positifs
Les optimisations agressives au cycle CPU près.
Section compile time




benchstat utilisée pour mesurer les performances, c'est une bonne pratique, calculer les statistiques. 
