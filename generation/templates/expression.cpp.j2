#ifndef EXPRESSION_{{nom | upper}}_CPP
#define EXPRESSION_{{nom | upper}}_CPP

#include "{{_include_expression_}}"

// Do not modify the code; this section is dynamically generated by the Jinja2 template.
Expression{{nom}}::Expression{{nom}}(ContextExpression& contexteExpression)
    : _contexteExpression(contexteExpression)
{}

Expression{{nom}}::~Expression{{nom}}()
{}

INode* Expression{{nom}}::construire(std::vector<Token>& equation)
{
    (void)equation;
    return nullptr;
}

#endif /* EXPRESSION_{{nom | upper}}_CPP */